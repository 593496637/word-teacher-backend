name: 🚀 Deploy to Cloudflare Workers (Official Mastra Method)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  # 添加手动触发功能
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: false
        default: 'production'
        type: choice
        options:
          - production
          - staging

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: 🏗️ Build and Deploy Mastra Backend (Official Method)
    
    steps:
      # 📥 检出代码
      - name: 📥 Checkout
        uses: actions/checkout@v4
        
      # 🟢 设置 Node.js (不使用缓存，因为没有 lock 文件)
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          # 暂时移除 cache，直到我们有 package-lock.json
          # cache: 'npm'
          
      # 📦 安装依赖 (使用 npm install 而不是 npm ci)
      - name: 📦 Install dependencies
        run: npm install
        
      # 🔧 类型检查
      - name: 🔧 Type check
        run: npx tsc --noEmit
        
      # 🏗️ 构建 Mastra 项目
      - name: 🏗️ Build Mastra project
        run: npx mastra build
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          
      # 🚀 使用官方 Mastra CLI 部署到 Cloudflare Workers
      - name: 🚀 Deploy using Mastra CLI
        run: npx mastra deploy
        if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          
      # 📄 上传构建产物
      - name: 📄 Upload build artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build-files
          path: .mastra/
          retention-days: 7
